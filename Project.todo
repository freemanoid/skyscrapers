{TODO : 
идея: при увеличении размера поля становится тяжело
сгенерировать более лёгкие условия (например с помощью 
максимальных видимостей), поэтому есть смысл задавать
изначально, для этого нужно сразу проставлять строку или столбец
и при генерации пропускать их + нужно не возвращать
сгенерированный UnitsArray, а передавать (вдруг в нём уже
проставлена строка или столбец) и добавлять уже 
сгенерированные строки и столбцы.}



